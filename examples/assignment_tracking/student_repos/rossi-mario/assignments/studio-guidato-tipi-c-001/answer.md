# Tipi interi in C

`int` rappresenta un intero con segno. Il range dipende dalla piattaforma, ma
nel codice posso controllare i limiti includendo `limits.h`.

Esempio:

```c
printf("%d\n", valore);
```
