# Query-bearing request fields and arbitrary headers are deliberately excluded:
# OAuth callbacks and untrusted header values may carry credentials.
log_format thebitlab '$remote_addr [$time_local] '
                     '"$request_method $uri $server_protocol" $status $body_bytes_sent '
                     'request_time=$request_time request_id=$request_id';
